# VoiceTranser

Voice input → AI-refined prompt → auto-paste to Claude Code.

全局热键按住说话，松开后自动转写、精炼、粘贴。

## Quick Start

```bash
cd VoiceTranser
uv sync
cp .env.example .env  # fill in API keys
uv run python -m voicetranser
```

Press and hold right Cmd key to speak, release to process.
